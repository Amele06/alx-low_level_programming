#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <string.h>

int _putchar(char c);
int main(int __attribute__((unused)) argc, char *argv[]);
int main(int argc, char *argv[]);
int _atoi(char *s);

#endif
